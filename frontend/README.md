# Starlight Staking Frontend

This is a Vite-powered React application built with `lucide-react` icons and a custom `Vanilla CSS` Glassmorphism layout.

## Setup
1. Run `npm install`
2. Run `npm run dev`
